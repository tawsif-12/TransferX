export const metadata = {
  title: 'TransferX API',
  description: 'Football Transfer Management System API',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
